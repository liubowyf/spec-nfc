# 接口设计说明

接口细节不足以直接实现时，先补这份设计。

至少写清：
- 提供方 / 消费方
- 请求结构
- 返回结构
- 错误码
- 兼容策略
- 鉴权、超时、重试、幂等

要求：
- 接口变更先写设计，再写实现
- 明确兼容策略，不允许默认“调用方自己适配”
- 接口设计要与 `specs/changes/<change-id>/spec.md` 和 `plan.md` 对齐

顺序：

1. 先写 `template.md`
2. 再用 `review-checklist.md` 自检
3. 再进入实现阶段
