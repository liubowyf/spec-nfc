# Plan

## Phase 1 — 冻结合同对象与 schema
- 定义 team/project/repo/change/integration 最小 schema
- 定义 `.specnfc/` 目录与 projection boundary
- 冻结 canonical phase 及 legacy alias

## Phase 2 — 重写协议语义骨架
- 重构 `init/status/doctor` 的产品语义与 JSON contract
- 定义 next-step protocol 输出
- 定义 repo contract / stage machine / doc index

## Phase 3 — skill-pack 与入口投影
- 定义 skill-pack manifest
- 定义 team source -> repo snapshot 流程
- 定义 `AGENTS.md/CLAUDE.md/.trae/opencode` 投影生成规则

## Phase 4 — 多层索引与对象联动
- 将项目记忆上提为多层索引系统
- 让 change / integration 进入 canonical phase 与 link graph
- 输出 repo/change/integration index

## Phase 5 — upgrade 与旧仓迁移
- 扩展 managed files
- 提供 schema migration / alias migration / projection regeneration
- 提供 old repo 渐进升级指引
