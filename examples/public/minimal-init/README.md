# 最小初始化示例

适合说明 `specnfc init` 之后最先看到的核心对象。

推荐命令：

```bash
specnfc init --cwd /path/to/repo
specnfc status --cwd /path/to/repo
```

初始化后重点关注：

- `.specnfc/`：控制面
- `.nfc/`：运行时与协作层
- `specs/`：正式 dossier
- `AGENTS.md / CLAUDE.md / .trae/rules/project_rules.md / opencode.json`：入口投影
